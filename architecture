Dinner Data

    Structure:

        Weeks object contains: 
            -Array of week objects
        
        Week object contains:
            -Array of date objects for the week
            -Array of dinner objects for each day
    
    Handling:

        Week creation:
            -on load:
                -create dates for current week
                -next/prev week selection:
                -if Week exists, load scheduled dinner Data
                -if Week doesn't exist, create new Week and add to Weeks

        Dinner scheduling:
            -for each day of the week:
                -drop down selection of dinner options:
                    -when dinner is selected
                    -dinner is added to the dinners array at the appropriate index in Week object





